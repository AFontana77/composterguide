/**
 * IndexNow key for composterguide.com.
 *
 * The matching key file is served at /fc840405c3cdc6aa3343824bd857151a.txt from public/. Bing verifies the
 * key by fetching that file, so the two must stay in step. One key per property:
 * a leaked key cannot then be used to submit URLs for a different site.
 */
export const INDEXNOW_KEY = "fc840405c3cdc6aa3343824bd857151a";
